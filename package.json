{
  "name": "chrome-input-improve",
  "version": "1.0.3",
  "private": true,
  "description": "Chrome extension that adds a floating optimize button to text inputs and uses an OpenAI-compatible API to improve the text.",
  "scripts": {
    "clean": "shx rm -rf dist release",
    "dev": "npm run build:dev && npm run watch",
    "build:dev": "npm run clean && npm run build:background:dev && npm run build:content:dev && npm run build:options:dev && npm run build:static",
    "build:background:dev": "esbuild src/background.ts --bundle --platform=browser --outfile=dist/background.js --format=esm --sourcemap",
    "build:content:dev": "esbuild src/contentScript.ts --bundle --platform=browser --outfile=dist/contentScript.js --format=esm --sourcemap",
    "build:options:dev": "esbuild src/options/options.ts --bundle --platform=browser --outfile=dist/options.js --format=esm --sourcemap",
    "watch": "npm run watch:background & npm run watch:content & npm run watch:options",
    "watch:background": "esbuild src/background.ts --bundle --platform=browser --outfile=dist/background.js --format=esm --sourcemap --watch",
    "watch:content": "esbuild src/contentScript.ts --bundle --platform=browser --outfile=dist/contentScript.js --format=esm --sourcemap --watch",
    "watch:options": "esbuild src/options/options.ts --bundle --platform=browser --outfile=dist/options.js --format=esm --sourcemap --watch",
    "build": "npm run build:release",
    "build:release": "npm run clean && npm run build:background:release && npm run build:content:release && npm run build:options:release && npm run build:static",
    "build:background:release": "esbuild src/background.ts --bundle --platform=browser --outfile=dist/background.js --format=esm --minify",
    "build:content:release": "esbuild src/contentScript.ts --bundle --platform=browser --outfile=dist/contentScript.js --format=esm --minify",
    "build:options:release": "esbuild src/options/options.ts --bundle --platform=browser --outfile=dist/options.js --format=esm --minify",
    "build:static": "shx mkdir -p dist && shx cp src/options/options.html dist/options.html && shx cp src/options/options.css dist/options.css",
    "bump:version": "node scripts/bump-extension-version.mjs",
    "pack": "npm run bump:version && npm run build:release && shx mkdir -p release && bestzip release/chrome-input-improve.zip manifest.json icon128.png dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "bestzip": "^2.2.1",
    "esbuild": "^0.24.0",
    "shx": "^0.3.4",
    "typescript": "^5.6.0"
  }
}
